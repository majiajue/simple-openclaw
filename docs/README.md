# simple-openclaw v2.0

`simple-openclaw` is an installation and operations wrapper for OpenClaw. It focuses on five control points:

- Install
- Doctor
- Probe
- Rollback
- Plugin governance

The repository is organized as a shell-based CLI scaffold so you can expand the wrapper without coupling users directly to raw OpenClaw commands.
